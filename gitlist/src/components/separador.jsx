import styled from 'styled-components'

const SeparadorStyled = styled.div`
    border-block-end: 1px solid var(--grey);
    margin-block: 1rem;
`

function Separador() {
    return (
        <SeparadorStyled>
        </SeparadorStyled>
    )
}

export default Separador
